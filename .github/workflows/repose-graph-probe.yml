name: Build PrayerClarity Repose graph probe

on:
  workflow_dispatch:
  push:
    branches:
      - research/rebalanced-1.0.20-baseline
    paths:
      - 'PrayerClarity.ReposeGraphProbe.csproj'
      - 'src/PrayerClarityReposeGraphProbe.cs'
      - '.github/workflows/repose-graph-probe.yml'

permissions:
  contents: read

concurrency:
  group: prayerclarity-repose-graph-probe-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Restore
        run: dotnet restore PrayerClarity.ReposeGraphProbe.csproj
      - name: Build probe DLL
        run: dotnet build PrayerClarity.ReposeGraphProbe.csproj -c Release --no-restore
      - name: Stage artifact
        id: artifact
        shell: bash
        run: |
          mkdir -p dist
          asset='PrayerClarity.ReposeGraphProbe.0.1.0.dll'
          cp 'bin/ReposeGraphProbe/PrayerClarity.ReposeGraphProbe.dll' "dist/$asset"
          hash=$(sha256sum "dist/$asset" | awk '{print $1}')
          echo "asset=$asset" >> "$GITHUB_OUTPUT"
          echo "sha256=$hash" >> "$GITHUB_OUTPUT"
          echo "PrayerClarity Repose graph probe 0.1.0 SHA-256: $hash"
      - name: Upload probe DLL
        uses: actions/upload-artifact@v4
        with:
          name: PrayerClarity-ReposeGraphProbe-0.1.0
          path: dist/${{ steps.artifact.outputs.asset }}
          if-no-files-found: error
          retention-days: 7
